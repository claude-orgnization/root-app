name: Deploy to GitHub Pages

on:
  push:
    branches: [master]

permissions:
  contents: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            qiita-ai/package-lock.json
            puzzle-app/package-lock.json
            github-trending/package-lock.json
            amida-app/package-lock.json

      - name: Install dependencies
        working-directory: qiita-ai
        run: npm ci

      - name: Build
        working-directory: qiita-ai
        run: npm run build

      - name: Install puzzle-app dependencies
        working-directory: puzzle-app
        run: npm ci

      - name: Build puzzle-app
        working-directory: puzzle-app
        run: npm run build

      - name: Install github-trending dependencies
        working-directory: github-trending
        run: npm ci

      - name: Build github-trending
        working-directory: github-trending
        run: npm run build

      - name: Install amida-app dependencies
        working-directory: amida-app
        run: npm ci

      - name: Build amida-app
        working-directory: amida-app
        run: npm run build

      - name: Prepare deploy directory
        run: |
          mkdir -p deploy/qiita-ai
          mkdir -p deploy/puzzle-app
          cp index.html deploy/index.html
          cp -r qiita-ai/dist/. deploy/qiita-ai/
          cp -r puzzle-app/dist/. deploy/puzzle-app/
          mkdir -p deploy/github-trending
          cp -r github-trending/dist/. deploy/github-trending/
          mkdir -p deploy/amida-app
          cp -r amida-app/dist/. deploy/amida-app/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy
